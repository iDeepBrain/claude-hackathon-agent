fastapi>=0.115.0
uvicorn[standard]>=0.30.0
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0
langchain-community>=0.3.0
langchain-mcp-adapters>=0.1.0
redis>=5.0.0
rapidfuzz>=3.9.0
httpx>=0.27.0
python-dotenv>=1.0.0
sse-starlette>=2.1.0
anthropic>=0.49.0
fastembed>=0.4.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
fakeredis[aioredis]>=2.20.0
apscheduler>=3.10
google-auth>=2.30.0
PyYAML>=6.0
# Postgres direct access — used ONLY by scripts/reset_local.py (the
# runtime path goes through MCP). Adding here keeps the dev tooling
# self-contained: `docker compose exec agent python scripts/reset_local.py`
# works without juggling a separate container.
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.30
